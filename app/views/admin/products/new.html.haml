- title t('.title')

- content_for :submenu do
  = render_cell 'admin/shop_menu', :show

- content_for :actions do
  = render 'admin/shared/actions/products'

%p!= t('.help')

= semantic_form_for @product, :url => admin_products_url, :html => { :multipart => true } do |form|

  = render 'form', :f => form

  = render 'admin/shared/form_actions', :back_url => admin_products_url, :button_label => :create